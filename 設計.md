# 日報自動送信システム 設計書

## 1. システム概要

```
┌─────────────────────────────────────────────────────────────────┐
│                        システム全体像                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐    push     ┌──────────┐   23:00 JST  ┌───────┐ │
│  │ Obsidian │ ─────────▶  │  GitHub  │ ───────────▶ │ Gmail │ │
│  │  Vault   │             │   Repo   │   Actions    │  SMTP │ │
│  └──────────┘             └──────────┘              └───────┘ │
│       │                        │                        │      │
│  Win/Mac/iPhone           Private Repo              送信先     │
│                                                    メール      │
└─────────────────────────────────────────────────────────────────┘
```

## 2. ディレクトリ構成

```
obsidian-vault/
├── .github/
│   └── workflows/
│       └── send-daily-report.yml    # GitHub Actions ワークフロー
├── 10_daily/                         # 日報フォルダ
│   └── YYYY/
│       └── MM/
│           └── YYYY-MM-DD-日報.md   # 日報ファイル
├── 20_poem/                          # ポエムフォルダ
├── 30_tech-memo/                     # 技術メモフォルダ
└── templates/
    └── daily-template.md             # 日報テンプレート
```

## 3. 日報ファイル仕様

### 3.1 ファイルパス規約

| 項目 | 仕様 |
|------|------|
| 基準フォルダ | `10_daily/` |
| パス形式 | `10_daily/YYYY/MM/YYYY-MM-DD-日報.md` |
| 日付基準 | JST（日本標準時） |
| 例 | `10_daily/2025/01/2025-01-18-日報.md` |

### 3.2 日報テンプレート（templates/daily-template.md）

```markdown
# {{date:YYYY-MM-DD}} 日報

## やったこと
- 

## 学び・気づき
- 

## 明日やること
- 

## メモ

```

## 4. GitHub Actions ワークフロー設計

### 4.1 ワークフロー概要

| 項目 | 設定値 |
|------|--------|
| ファイル名 | `.github/workflows/send-daily-report.yml` |
| トリガー | schedule (cron) + workflow_dispatch |
| 実行時刻 | 毎日 23:00 JST（= 14:00 UTC） |
| 実行環境 | ubuntu-latest |
| タイムゾーン | Asia/Tokyo |

### 4.2 cron 設定

```yaml
# 23:00 JST = 14:00 UTC
schedule:
  - cron: '0 14 * * *'
```

### 4.3 処理フロー

```
┌─────────────────────────────────────────────────────────────┐
│                    ワークフロー処理フロー                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ① リポジトリをチェックアウト                                │
│           │                                                 │
│           ▼                                                 │
│  ② 当日の日付（JST）を取得                                   │
│           │                                                 │
│           ▼                                                 │
│  ③ 日報ファイルパスを生成                                    │
│     10_daily/YYYY/MM/YYYY-MM-DD-日報.md                     │
│           │                                                 │
│           ▼                                                 │
│  ④ ファイル存在チェック ──────────────────┐                 │
│           │                              │                 │
│        存在する                       存在しない            │
│           │                              │                 │
│           ▼                              ▼                 │
│  ⑤ メール送信              ジョブ失敗（exit 1）            │
│           │                                                 │
│           ▼                                                 │
│  ⑥ 完了                                                     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.4 ワークフロー定義（send-daily-report.yml）

```yaml
name: 日報メール送信

on:
  schedule:
    # 毎日 23:00 JST（= 14:00 UTC）に実行
    - cron: '0 14 * * *'
  workflow_dispatch:
    # 手動実行も可能にする

env:
  TZ: Asia/Tokyo

jobs:
  send-daily-report:
    runs-on: ubuntu-latest
    
    steps:
      # ① リポジトリをチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v4

      # ② 当日の日付（JST）と日報ファイルパスを生成
      - name: Set date and file path
        id: date
        run: |
          YEAR=$(date '+%Y')
          MONTH=$(date '+%m')
          DATE=$(date '+%Y-%m-%d')
          FILE_PATH="10_daily/${YEAR}/${MONTH}/${DATE}-日報.md"
          
          echo "date=${DATE}" >> $GITHUB_OUTPUT
          echo "file_path=${FILE_PATH}" >> $GITHUB_OUTPUT
          echo "📅 対象日付: ${DATE}"
          echo "📄 ファイルパス: ${FILE_PATH}"

      # ③ 日報ファイルの存在チェック
      - name: Check if daily report exists
        id: check
        run: |
          FILE_PATH="${{ steps.date.outputs.file_path }}"
          
          if [ -f "$FILE_PATH" ]; then
            echo "✅ 日報ファイルが見つかりました: ${FILE_PATH}"
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "❌ 日報ファイルが見つかりません: ${FILE_PATH}"
            echo "exists=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      # ④ メール送信
      - name: Send email
        if: steps.check.outputs.exists == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          secure: true
          username: ${{ secrets.GMAIL_USER }}
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: "[日報] ${{ steps.date.outputs.date }}"
          to: ${{ secrets.MAIL_TO }}
          from: ${{ secrets.GMAIL_USER }}
          body: |
            本日の日報をお送りします。
            
            ─────────────────────────────
            
            ${{ steps.read-file.outputs.content }}
            
            ─────────────────────────────
            
            ※ このメールは GitHub Actions により自動送信されています。
          content_type: text/plain

      # ④-補足: ファイル内容を読み込む（メール本文用）
      - name: Read daily report content
        if: steps.check.outputs.exists == 'true'
        id: read-file
        run: |
          FILE_PATH="${{ steps.date.outputs.file_path }}"
          
          # ファイル内容を環境変数に設定（改行対応）
          {
            echo 'content<<EOF'
            cat "$FILE_PATH"
            echo 'EOF'
          } >> $GITHUB_OUTPUT
```

**注意**: 上記のワークフローでは `read-file` ステップが `send-email` より後に定義されていますが、実際には順序を入れ替える必要があります。正しい順序版は次のセクションで示します。

### 4.5 ワークフロー定義（修正版・完全版）

```yaml
name: 日報メール送信

on:
  schedule:
    # 毎日 23:00 JST（= 14:00 UTC）に実行
    - cron: '0 14 * * *'
  workflow_dispatch:
    # 手動実行も可能にする

env:
  TZ: Asia/Tokyo

jobs:
  send-daily-report:
    runs-on: ubuntu-latest
    
    steps:
      # ① リポジトリをチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v4

      # ② 当日の日付（JST）と日報ファイルパスを生成
      - name: Set date and file path
        id: date
        run: |
          YEAR=$(date '+%Y')
          MONTH=$(date '+%m')
          DATE=$(date '+%Y-%m-%d')
          FILE_PATH="10_daily/${YEAR}/${MONTH}/${DATE}-日報.md"
          
          echo "year=${YEAR}" >> $GITHUB_OUTPUT
          echo "month=${MONTH}" >> $GITHUB_OUTPUT
          echo "date=${DATE}" >> $GITHUB_OUTPUT
          echo "file_path=${FILE_PATH}" >> $GITHUB_OUTPUT
          
          echo "📅 対象日付: ${DATE}"
          echo "📄 ファイルパス: ${FILE_PATH}"

      # ③ 日報ファイルの存在チェック
      - name: Check if daily report exists
        id: check
        run: |
          FILE_PATH="${{ steps.date.outputs.file_path }}"
          
          if [ -f "$FILE_PATH" ]; then
            echo "✅ 日報ファイルが見つかりました: ${FILE_PATH}"
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "❌ 日報ファイルが見つかりません: ${FILE_PATH}"
            echo "exists=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      # ④ ファイル内容を読み込む
      - name: Read daily report content
        if: steps.check.outputs.exists == 'true'
        id: read-file
        run: |
          FILE_PATH="${{ steps.date.outputs.file_path }}"
          
          # ファイル内容を環境変数に設定（改行対応）
          {
            echo 'content<<EOF'
            cat "$FILE_PATH"
            echo 'EOF'
          } >> $GITHUB_OUTPUT

      # ⑤ メール送信
      - name: Send email
        if: steps.check.outputs.exists == 'true'
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          secure: true
          username: ${{ secrets.GMAIL_USER }}
          password: ${{ secrets.GMAIL_APP_PASSWORD }}
          subject: "[日報] ${{ steps.date.outputs.date }}"
          to: ${{ secrets.MAIL_TO }}
          from: ${{ secrets.GMAIL_USER }}
          body: |
            本日の日報をお送りします。
            
            ─────────────────────────────
            
            ${{ steps.read-file.outputs.content }}
            
            ─────────────────────────────
            
            ※ このメールは GitHub Actions により自動送信されています。
          content_type: text/plain
```

## 5. GitHub Secrets 設定

### 5.1 必要な Secrets 一覧

| Secret 名 | 説明 | 設定例 |
|-----------|------|--------|
| `GMAIL_USER` | 送信元 Gmail アドレス | `your-email@gmail.com` |
| `GMAIL_APP_PASSWORD` | Gmail アプリパスワード | `xxxx xxxx xxxx xxxx` |
| `MAIL_TO` | 送信先アドレス（複数可） | `addr1@example.com,addr2@example.com` |

### 5.2 複数送信先の設定方法

`MAIL_TO` にカンマ区切りで複数アドレスを設定：

```
recipient1@example.com,recipient2@example.com,recipient3@example.com
```

## 6. Gmail 設定手順

### 6.1 2段階認証の有効化

1. Google アカウントにログイン
2. 「セキュリティ」→「2段階認証プロセス」
3. 画面の指示に従って有効化

### 6.2 アプリパスワードの発行

1. Google アカウント → 「セキュリティ」
2. 「2段階認証プロセス」をクリック
3. ページ下部の「アプリ パスワード」をクリック
4. アプリ名を入力（例：`GitHub Actions`）
5. 「作成」をクリック
6. 表示された16文字のパスワードをコピー

**重要**: このパスワードは一度しか表示されません。必ず安全な場所に保存してください。

## 7. セットアップ手順

### 7.1 手順チェックリスト

```
□ 1. Gmail で2段階認証を有効化
□ 2. Gmail でアプリパスワードを発行
□ 3. GitHub リポジトリを作成（Private）
□ 4. リポジトリの Settings → Secrets and variables → Actions
□ 5. 以下の Secrets を追加:
     □ GMAIL_USER
     □ GMAIL_APP_PASSWORD
     □ MAIL_TO
□ 6. .github/workflows/send-daily-report.yml を作成・push
□ 7. Obsidian Vault を GitHub に接続
□ 8. 日報フォルダ構成を作成（10_daily/YYYY/MM/）
□ 9. テスト: workflow_dispatch で手動実行して動作確認
```

### 7.2 動作確認手順

1. テスト用の日報ファイルを作成
   ```
   10_daily/2025/01/2025-01-18-日報.md
   ```

2. GitHub に push

3. Actions タブ → 「日報メール送信」→ 「Run workflow」で手動実行

4. メールが届くことを確認

## 8. エラーハンドリング

### 8.1 想定されるエラーと対処

| エラー | 原因 | 対処 |
|--------|------|------|
| ファイルが見つからない | 日報未作成 or パス不一致 | 日報を作成して push |
| SMTP 認証エラー | アプリパスワード誤り | Secrets を再設定 |
| 送信失敗 | Gmail の送信制限 | 時間をおいて再実行 |

### 8.2 ジョブ失敗時の通知

GitHub の設定で、ワークフロー失敗時にメール通知を受け取れます：

1. GitHub → Settings → Notifications
2. 「Actions」セクションで通知設定を有効化

## 9. 拡張案

### 9.1 週報・月報への応用

ファイルパターンと cron を変更することで対応可能：

```yaml
# 週報（毎週金曜 18:00 JST）
schedule:
  - cron: '0 9 * * 5'

# 月報（毎月末 23:00 JST）
schedule:
  - cron: '0 14 28-31 * *'
```

### 9.2 Markdown → HTML 変換

メール本文を HTML 化したい場合：

```yaml
- name: Convert Markdown to HTML
  run: |
    npm install -g marked
    marked "${{ steps.date.outputs.file_path }}" > report.html
```

### 9.3 添付ファイル対応

日報を添付ファイルとして送信：

```yaml
- name: Send email with attachment
  uses: dawidd6/action-send-mail@v3
  with:
    # ... 他の設定 ...
    attachments: ${{ steps.date.outputs.file_path }}
```

## 10. 運用上の注意点

1. **push タイミング**: 23:00 JST までに当日の日報を push しておく
2. **フォルダ作成**: 月が変わったら新しいフォルダ（例：`10_daily/2025/02/`）を作成
3. **Secrets の管理**: アプリパスワードは定期的に更新を推奨
4. **送信制限**: Gmail は1日あたり500通の送信制限あり（通常運用では問題なし）