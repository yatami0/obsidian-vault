name: 日報メール送信

on:
  schedule:
    # 毎日 23:00 JST（= 14:00 UTC）に実行
    - cron: '0 14 * * *'
  workflow_dispatch:
    # 手動実行も可能にする

env:
  TZ: Asia/Tokyo
  LOOKBACK_DAYS: 3  # 直近何日分を走査するか

jobs:
  send-daily-report:
    runs-on: ubuntu-latest

    steps:
      # ① リポジトリをチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # ② Python 環境のセットアップ
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ③ 依存パッケージのインストール
      - name: Install dependencies
        run: |
          pip install pyyaml

      # ④ 未送信日報の検出と送信
      - name: Send unsent daily reports
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          LOOKBACK_DAYS: ${{ env.LOOKBACK_DAYS }}
        run: |
          python .github/scripts/send_daily_report.py

      # ⑤ 送信済みフラグの変更をコミット・プッシュ
      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # 変更があるかチェック
          if git diff --quiet 10_daily/; then
            echo "📝 送信済みフラグの変更はありません"
          else
            echo "📝 送信済みフラグを更新してコミットします"
            git add 10_daily/
            git commit -m "chore: 日報送信済みフラグを更新"
            git push
            echo "✅ プッシュ完了"
          fi
