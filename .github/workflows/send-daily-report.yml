name: 日報メール送信

on:
  schedule:
    # 毎日 23:00 JST（= 14:00 UTC）に実行
    - cron: '0 14 * * *'
  workflow_dispatch:
    # 手動実行も可能にする

env:
  TZ: Asia/Tokyo

jobs:
  send-daily-report:
    runs-on: ubuntu-latest

    steps:
      # ① リポジトリをチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v4

      # ② 当日の日付（JST）と日報ファイルパスを生成
      - name: Set date and file path
        id: date
        run: |
          YEAR=$(date '+%Y')
          MONTH=$(date '+%m')
          DATE=$(date '+%Y-%m-%d')
          FILE_PATH="10_daily/${YEAR}/${MONTH}/${DATE}.md"

          echo "year=${YEAR}" >> $GITHUB_OUTPUT
          echo "month=${MONTH}" >> $GITHUB_OUTPUT
          echo "date=${DATE}" >> $GITHUB_OUTPUT
          echo "file_path=${FILE_PATH}" >> $GITHUB_OUTPUT

          echo "📅 対象日付: ${DATE}"
          echo "📄 ファイルパス: ${FILE_PATH}"

      # ③ 日報ファイルの存在チェック
      - name: Check if daily report exists
        id: check
        run: |
          FILE_PATH="${{ steps.date.outputs.file_path }}"

          if [ -f "$FILE_PATH" ]; then
            echo "✅ 日報ファイルが見つかりました: ${FILE_PATH}"
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "❌ 日報ファイルが見つかりません: ${FILE_PATH}"
            echo "exists=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      # ④ Python 環境のセットアップ
      - name: Set up Python
        if: steps.check.outputs.exists == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # ⑤ メール送信（Pythonスクリプトで直接送信）
      - name: Send email
        if: steps.check.outputs.exists == 'true'
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          FILE_PATH: ${{ steps.date.outputs.file_path }}
          DATE: ${{ steps.date.outputs.date }}
        run: |
          python .github/scripts/send_daily_report.py
