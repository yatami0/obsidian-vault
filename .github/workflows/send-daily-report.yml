name: 日報メール送信

on:
  schedule:
    # 毎日 23:00 JST（= 14:00 UTC）に実行
    - cron: '0 14 * * *'
  workflow_dispatch:
    # 手動実行も可能にする

permissions:
  id-token: write  # OIDC トークン取得に必要
  contents: read

env:
  TZ: Asia/Tokyo

jobs:
  send-daily-report:
    runs-on: ubuntu-latest

    steps:
      # ① リポジトリをチェックアウト
      - name: Checkout repository
        uses: actions/checkout@v4

      # ② 当日の日付（JST）と日報ファイルパスを生成
      - name: Set date and file path
        id: date
        run: |
          YEAR=$(date '+%Y')
          MONTH=$(date '+%m')
          DATE=$(date '+%Y-%m-%d')
          FILE_PATH="10_daily/${YEAR}/${MONTH}/${DATE}-日報.md"

          echo "year=${YEAR}" >> $GITHUB_OUTPUT
          echo "month=${MONTH}" >> $GITHUB_OUTPUT
          echo "date=${DATE}" >> $GITHUB_OUTPUT
          echo "file_path=${FILE_PATH}" >> $GITHUB_OUTPUT

          echo "📅 対象日付: ${DATE}"
          echo "📄 ファイルパス: ${FILE_PATH}"

      # ③ 日報ファイルの存在チェック
      - name: Check if daily report exists
        id: check
        run: |
          FILE_PATH="${{ steps.date.outputs.file_path }}"

          if [ -f "$FILE_PATH" ]; then
            echo "✅ 日報ファイルが見つかりました: ${FILE_PATH}"
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "❌ 日報ファイルが見つかりません: ${FILE_PATH}"
            echo "exists=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      # ④ ファイル内容を読み込む
      - name: Read daily report content
        if: steps.check.outputs.exists == 'true'
        id: read-file
        run: |
          FILE_PATH="${{ steps.date.outputs.file_path }}"

          # ファイル内容を環境変数に設定（改行対応）
          {
            echo 'content<<EOF'
            cat "$FILE_PATH"
            echo 'EOF'
          } >> $GITHUB_OUTPUT

      # ⑤ AWS認証（OIDC）
      - name: Configure AWS credentials
        if: steps.check.outputs.exists == 'true'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::050752605572:role/github-actions-ses-role
          aws-region: ap-northeast-1

      # ⑥ AWS SESでメール送信
      - name: Send email via SES
        if: steps.check.outputs.exists == 'true'
        env:
          EMAIL_CONTENT: ${{ steps.read-file.outputs.content }}
          EMAIL_DATE: ${{ steps.date.outputs.date }}
        run: |
          # メール本文を作成
          BODY="本日の日報

          ─────────────────────────────

          ${EMAIL_CONTENT}

          ─────────────────────────────

          ※ このメールは GitHub Actions により自動送信されています。"

          # AWS SESでメール送信
          aws ses send-email \
            --from "takumik9823@gmail.com" \
            --destination "ToAddresses=takumik9823@gmail.com" \
            --message "Subject={Data='[日報] ${EMAIL_DATE}',Charset='UTF-8'},Body={Text={Data='${BODY}',Charset='UTF-8'}}"

          echo "✅ メール送信完了"
